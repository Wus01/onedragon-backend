<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="restapi.prac.repository.mapper.MyPageMapper">

    <select id="selectApplicationsByUserId"  parameterType="string"  resultType="restapi.prac.model.dto.ApplicationDto">

        SELECT
            S.store_id AS storeId,
            S.store_nm AS storeNm,
            COALESCE(C.status, '01') AS status,
            C.crr_strt_date AS applyDate
        FROM crr_hstr C
                 JOIN store_info S ON C.store_id = S.store_id
        WHERE C.user_id = #{userId}
          AND C.del_yn = false
        ORDER BY C.rgst_date DESC

    </select>

</mapper>
